import { Color } from '@rc-component/color-picker';
import React from 'react';

const Header =() =>{
  return (
    <div style={{height:70, background: "linear-gradient(to right, rgb(70, 53, 70), white)", color:"black", textAlign:"center"}}>
        <h1>header</h1>
    </div>
  );
}

export default Header;
